# CLAUDE.md

Project guidance lives in [`AGENTS.md`](../AGENTS.md) at the repo root —
that's the canonical document for any agent (Claude Code, Codex, Cursor,
Aider, …). Read it first.

This file exists only as a pointer so editors that look for `CLAUDE.md`
specifically still find the guide. Don't add Claude-only guidance here —
keep everything in `AGENTS.md` so it stays in sync across tools.
