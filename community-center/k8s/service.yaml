# Generated by the Corpo Valley portal from your enabled capabilities. One
# Service per capability, all selecting the project's single pod but each
# forwarding to that capability's container port. The Ingress path-routes to
# these. Don't hand-edit — toggle capabilities in the portal.
---
apiVersion: v1
kind: Service
metadata:
  name: {{SLUG}}
  namespace: {{SLUG}}
spec:
  selector:
    app: {{SLUG}}
  ports:
    - name: http
      port: 80
      targetPort: http-site
---
apiVersion: v1
kind: Service
metadata:
  name: {{SLUG}}-api
  namespace: {{SLUG}}
spec:
  selector:
    app: {{SLUG}}
  ports:
    - name: http
      port: 80
      targetPort: http-api
---
apiVersion: v1
kind: Service
metadata:
  name: {{SLUG}}-files
  namespace: {{SLUG}}
spec:
  selector:
    app: {{SLUG}}
  ports:
    - name: http
      port: 80
      targetPort: http-files
---
apiVersion: v1
kind: Service
metadata:
  name: {{SLUG}}-mcp
  namespace: {{SLUG}}
spec:
  selector:
    app: {{SLUG}}
  ports:
    - name: http
      port: 80
      targetPort: http-mcp
